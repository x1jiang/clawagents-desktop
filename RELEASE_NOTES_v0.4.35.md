# ClawAgents Desktop v0.4.35

## Summary

- **Invented SQL tables are rejected.** If Gemini writes a count matrix or claims it executed SQL without calling `execute` / `use_skill` this turn, the harness sends it back to run a real tool and quote only that output.

## Install

Download the `ClawAgents Desktop_0.4.35_aarch64.dmg` asset below, open it, and drag ClawAgents Desktop to Applications.
