backend/.venv/
backend/.ruff_cache/
backend/.pytest_cache/
backend/**/__pycache__/
backend/*.log
backend/*.egg-info/
.env
.env.*
!.env.example
backend/build/
backend/dist/
.DS_Store
ui/node_modules/
ui/dist/
ui/src-tauri/target/
ui/src-tauri/Cargo.lock
ui/src-tauri/gen/
ui/*.tsbuildinfo
ui/vite.config.d.ts
ui/vite.config.js
ui/.npmrc
ui/pnpm-workspace.yaml

# Local agent / session state — never publish
.clawagents/
.omx/

# Release staging (DMGs uploaded via GitHub Releases, not git)
/dist-release/
*.dmg

# Apple signing materials — never commit private keys / CSRs / .cer
/certs/
*.p8
*.p12
*.cer
*.certSigningRequest

# Local fork snapshots — never ship
backend/.fork_backup*/
**/.fork_backup*/
